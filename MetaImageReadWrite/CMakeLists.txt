IF(WIN32)
  IF(NOT BORLAND)
    SET(ITK_MIRW_WSOCK32 wsock32)
  ENDIF(NOT BORLAND)
ENDIF(WIN32)

ADD_EXECUTABLE(MetaImageReadWrite MetaImageReadWrite.cxx)
TARGET_LINK_LIBRARIES(MetaImageReadWrite ITKBasicFilters ITKIO
                      ${ITK_MIRW_WSOCK32})
